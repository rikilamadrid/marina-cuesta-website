# Force the public npm registry for this project. Never resolve packages from
# the BLDR/Azure private feed — that pinned private tarball URLs into the
# lockfile and broke Vercel/CI installs (E401). Keep this file committed.
registry=https://registry.npmjs.org/
